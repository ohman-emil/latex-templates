% Packages
\usepackage[T1]{fontenc}
\usepackage[pdfusetitle]{hyperref}
\usepackage{csquotes}
\usepackage{fancyhdr}
\usepackage[total={6in,8in}]{geometry}

% Config
%\setlength{\parindent}{0pt}
%\setlength{\parskip}{\baselineskip}

% Fancy Header
\pagestyle{fancy}
\fancyhead{}
\makeatletter\lhead{\@title}\makeatother
\makeatletter\rhead{\@author, \@date}\makeatother

% Add PDF-metadata
\makeatletter
\hypersetup{
    colorlinks,
    citecolor=black,
    filecolor=black,
    linkcolor=black,
    urlcolor=blue
}
\makeatother

